git restore --source 커밋아이디 파일명 // 특정 시점으로 파일을 복구

git revert 커밋아이디 // 특정 커밋을 취소하는 명령어

git reset 커밋아이디 // 과거로 되돌리는 명령어


auto import
es7
eslint
live server


git add .

git commit -m "커밋 메세지"

git push
PR :  Pull Request

 git checkout -b travel

 git add .
 git commit -m "여행 페이지 공부"
 git push

컨플릭트 났을 때 해결
 git checkout 컨플릭트 나는 쪽 브랜치로 이동
 git pull
 git checkout 컨플릭트를 해결해야 하는 브랜치로 이동
